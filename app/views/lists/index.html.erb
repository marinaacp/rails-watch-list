<h1> Orgonize your movies </h1>
<div>
  <%= link_to "Create a list of movies", new_list_path, class: "btn btn-primary" %>
  <%= link_to "Create a bookmark", new_list_bookmark_path(@list), class: "btn btn-primary" %>
</div>

<div>
  <% @lists.each do |list| %>
    <%= link_to list_path(list) do %>
      <div><%= list.name %></div>
    <% end %>
  <% end %>
</div>
